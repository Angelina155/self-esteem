import { observer } from 'mobx-react-lite'
import {Col}  from "react-bootstrap";


const DiaryItem = observer(() => {

    return (
        <Col md={3}>
        </Col>
    );
});

export default DiaryItem